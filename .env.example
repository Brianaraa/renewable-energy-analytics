# .env.example — copy to .env and fill in your values
# Never commit .env to git!

# Optional: Mapbox token for premium map tiles
# Get one free at https://account.mapbox.com/
MAPBOX_TOKEN=

# Optional: adjust log level
LOG_LEVEL=INFO
